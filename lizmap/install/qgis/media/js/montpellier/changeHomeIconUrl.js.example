lizMap.events.on({
    'uicreated':function(evt){

        var newHomeLink = 'http://3liz.com';
        // Replace home icon URL
       $('#content li.projects a').attr('href', newHomeLink);

    }
});
